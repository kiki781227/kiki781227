# .github/workflows/pacman.yml
name: GitHub Pacman Contribution Graph

on:
  schedule:
    - cron: '0 0 * * *'       # tous les jours à minuit UTC
  workflow_dispatch:         # lancement manuel
  push:
    branches:
      - main

jobs:
  generate:
    name: Génération & publication du graphe Pacman
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Install pacman-canvas
        run: npm install -g pacman-canvas

      - name: 🎮 Generate Pacman Contribution Graph
        run: |
          pacman-canvas \
            --username ${{ github.repository_owner }} \
            --output-dir ./dist \
            --light-file pacman.svg \
            --dark-file pacman-dark.svg

      - name: 🚀 Deploy to Output Branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: output
          commit_message: "🔄 Update Pacman contribution graph [skip ci]"
